import request from "supertest";

describe("Test for delete case route", () => {
  it("Checks for 200 and deleted case", () => {});
  it("Checks for 404 case not found", () => {});
  it("Checks for 401 for not authorized", () => {});
  it("Checks for 400 for invalid case number", () => {});
});
