import request from "supertest";

import { app } from "../../app";

describe("Test for returning a specific case", () => {
  it("Checks for 200 and returns case", () => {});

  it("Checks for 400 for invalid caseNo", () => {});

  it("Checks for 401 for unauthorized request", () => {});

  it("Checks for 404 for unfound case", () => {});
});
